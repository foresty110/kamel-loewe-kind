<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kamel.board.mapper.CommentMapper">

    <select id="findAllByBoardId"
            parameterType="Long"
            resultType="Comment">
        SELECT id,
               board_id,
               author,
               content,
               created_at
        FROM comment
        WHERE board_id = #{id}
    </select>

    <select id="findById"
            parameterType="Long"
            resultType="Comment">
        SELECT id,
               board_id,
               author,
               content,
               created_at
        FROM comment
        WHERE id = #{id}
    </select>

    <insert id="insert"
            parameterType="Comment"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO comment (
            board_id,
            author,
            content
        ) VALUES (
                     #{boardId},
                     #{author},
                     #{content}
                 )
    </insert>
</mapper>
